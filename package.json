{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "fmt": "prettier --write --ignore-unknown --ignore-path=.gitignore ."
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.3.0",
    "nuxt": "^3.1.0",
    "prettier": "^2.8.3",
    "prettier-plugin-tailwindcss": "^0.2.2"
  }
}
